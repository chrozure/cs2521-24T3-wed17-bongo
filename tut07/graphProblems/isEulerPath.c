#include <stdbool.h>

#include "Graph.h"

struct edge {
    int v;
    int w;
};

bool isEulerPath(Graph g, struct edge e[], int nE);



bool isEulerPath(Graph g, struct edge e[], int nE) {
    return false;
}
